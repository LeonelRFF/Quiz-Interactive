# Carpeta de construcción (se genera automáticamente)
/_build

# Dependencias de Node (se instalan con 'npm install')
/node_modules

# Archivo de bloqueo de versiones de npm
/package-lock.json

# Archivos del sistema operativo y de editores
.DS_Store
Thumbs.db
.vscode/
.idea/

# Paquetes de Anki (solo van en los Releases de GitHub)
*.apkg